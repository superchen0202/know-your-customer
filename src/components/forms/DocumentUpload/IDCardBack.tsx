import { useController } from 'react-hook-form';
import { type UploadDocuments } from '@/components/forms/DocumentUpload/schema';

const IDCardBack = () => {
  // const { field, fieldState } = useController<UploadDocuments>({ name: 'idBack' });

  return <>IDCardBack</>;
};

export default IDCardBack;
